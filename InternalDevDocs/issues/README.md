# VBScript.NET 问题登记（issues 层）

本文档登记 **VBScript.NET 产品自身**发现的 bug / 缺陷（issues 层）。与 `proposals/`（能力增强）、`meetings/`（会议评估）、`spec/`（规范）分离：issue 是**既存行为与预期不符**的记录，给出症状、根因、预期行为与修复方向。

## 目录组织

- `issues/` ↔ 根目录登记 Open / In Progress 的 bug。
- 修复验证通过后：在 issue 内标记 **Fixed** 并注明修复 commit；若涉及能力变更，转 `proposals/` 评估。

## 问题清单

| # | 文件 | 问题 | 状态 |
|---|------|------|------|
| 01 | `issue-vbx-load-span-shift.md` | vbx `#Load` 文本内联导致后续 TextSpan 漂移（预期：与 C# `#load` 一样零漂移，独立树合并） | **Fixed**（2026-08-15） |
| 02 | `issue-scripting-xml-linq-reference.md` | Scripting 63 失败：net10.0 把 Xml.Linq 移到 `System.Private.Xml.Linq`，`IncludeInternalXmlHelper` 嵌入 helper 树但绑定缺直接引用 → 每个脚本提交 24 个 BC30002 | **Fixed**（2026-08-20） |
| 03 | `issue-vbi-imports-switch-nre.md` | vbi `/imports:<非法值>` 抛 `NullReferenceException` 栈、`/imports:` 诊断丢失（`ParseGlobalImports` 把 `Nothing` 塞进 `GlobalImports`，宿主 `GetScriptOptions` 在 Errors 门之前触发 `GetImports` NRE） | **Fixed**（2026-09-10） |
| 04 | `issue-script-top-level-extension-method-crash.md` | 脚本顶层 `<Extension>` 成员漏写 `Shared` 时无编译诊断：Debug 构建被 `Debug.Assert(Me.IsShared)` 断言终止，Release 落到 codegen NRE（`AllowsExtensionMethods` 把脚本类与标准模块并列放行，而「扩展方法必须 `Shared`」在模块内不可能被违反，故从无用户可见诊断） | **Fixed**（48d8edb） |
| 05 | `issue-submission-shared-field-initializer-typeload.md` | 提交里顶层 `Shared` 字段带初始化器 → 宿主 `TypeLoadException`（`IsSubmissionConstructor` 不看 `IsShared`，共享提交构造器被塞进含 `Me`/`submissionArray` 的实例初始化体）；编译期零诊断 | **Fixed**（48d8edb） |
| 06 | `issue-script-shared-field-await-initializer-crash.md` | 脚本顶层 `Shared` 字段初始化器含 `Await` → 编译器断言终止（`AwaitOperator` 存活到 codegen，落 `EmitExpression.vb:206-209` 的 `Case Else` 抛 `UnexpectedValue`；`EXITCODE=35`）。**非共享**同形状正常（已运行对照）⇒ `Shared` 特有。VB 缺 C# CS8100「静态字段初始化器不许 `await`」的对应诊断 ⇒ 从「报错」变「崩」 | **Fixed**（48d8edb） |
| 07 | `issue-submission-implicit-type-member-asserts.md` | 提交类顶层 `Event` / `WithEvents` 成员 → Debug 断言终止（提交类由 `ImplicitNamedTypeSymbol` 承载，`IsImplicitlyDeclared` 报 True，撞 `SynthesizedEventAccessorSymbol.vb:495` / `SourceWithEventsBackingFieldSymbol.vb:66` 的 `Not ContainingType.IsImplicitlyDeclared` 断言；`EXITCODE=35`）。**与 `Shared` 无关**（实例形状同样终止，已运行对照）；嵌套类里的同形状正常 | **Fixed**（48d8edb） |
| 08 | `issue-submission-shared-member-implicit-me.md` | 提交类的共享成员不报 BC30369：隐式 `Me` 到实例成员 → 共享方法体编译零诊断、运行期 `InvalidProgramException`（`EXITCODE=58`）；共享字段初始化器同形状则先撞 issue 05。根因 `Binder_Expressions.vb:2257-2270` 以注释「No code in a script class is shared」为前提，对脚本类的**任何**隐式 `Me` 一律 `Return True`，永不走到 `:2272-2278` 的 BC30369。**普通类同形状报 BC30369**（已运行对照，两形状） | **Fixed**（48d8edb） |
| 09 | `issue-initializer-diagnostic-does-not-gate-emit.md` | 初始化器里的「`Await` 不在 async 上下文」诊断（BC36937）**报了但不阻止发射**：嵌套类型（**与 `Shared` 无关**）里字段/属性初始化器含 `Await` → Debug 发射期断言终止（`EXITCODE=35`）且**零诊断输出**。根因：初始化器诊断经 `MethodCompiler.vb:599-607` 写进全局 `_diagnostics`，而逐方法发射门（`:1272` 的 `hasErrors`）只看 `diagsForCurrentMethod`（`GetInstance(template)` 的空袋，`BindingDiagnosticBag.vb:50-52`）与 bound 节点错误标志（`Binder_Expressions.vb:4742-4744` 只报不标）；判别性实证：同形状换 `Await 5`（操作数带错）就正常报 BC36937+BC36930、不崩。**修法取「让发射门看见初始化器诊断」：两个初始化器桶各自绑到独立诊断袋、绑完合并回 `_diagnostics`（`MethodCompiler.vb:599-623`），`Binder_Initializers.vb:51-55` 的 `HasAnyErrors` 增加 `bindingReportedErrors` 入参** | **Fixed**（d7b0fc8） |
| 10 | `issue-script-top-level-await-in-try-crash.md` | 脚本顶层 `Try`/`Catch`/`Finally`/`SyncLock` 里写 `Await` → **`BC36943` 判据未生效**（判据在 `Binder_Statements.vb:602-610`，只在 `BindMethodBlock`（`:291`/`:330`）里跑，而脚本顶层语句不在 `<Initialize>` 的方法体里——`SynthesizedInteractiveInitializerMethod.vb:135-142` 的方法体是只含 `BoundLabelStatement(ExitLabel)` 的空壳）。**三种子形状、两种后果**：`Catch`/`Finally` → 编译器 `NullReferenceException`（栈到共享 `CodeGen.ILBuilder.BlockedBranchDestinationSlow`，`ILBuilder.cs:413`，`EXITCODE=3`）且**零诊断输出**；**`SyncLock` → 不崩**，编译通过、运行期抛 `SynchronizationLockException`（`Monitor.Exit_Slowpath`，`EXITCODE=24`，**零编译期提示**、`Await` 之后的受保护代码在不持锁线程上执行）。**预期一律报 BC36943**（消息本身即同时点名三类块）。**判别性对照（已运行）**：同形状放进普通 `Async Function` → 正常报 BC36943、`EXIT=1`、不崩 ⇒ **脚本模式特有** | **Fixed**（48d8edb） |
| 11 | `issue-script-top-level-goto-await-crash.md` | 脚本顶层 `GoTo` 跨 `Await` → 编译器 `NullReferenceException`（栈到共享 `CodeGen.ILBuilder.BasicBlock.ShortenBranches`，`BasicBlock.cs:325`，`EXITCODE=3`）。**判别性对照（已运行）**：同形状放进普通 `Async Function` → **正常编译通过、无错误无崩溃** ⇒ 该形状合法，**脚本模式特有**。**根因已定位且与 #10 不同源**：`SourceMemberContainerTypeSymbol.vb:2613-2615` **显式丢弃顶层 `LabelStatement`**（带上游 `TODO (tomat)`）⇒ 真正的触发条件是「顶层标签」，**不带 `Await` 的顶层 `GoTo` 同样崩**（已实测）。**修复后会新增行为变化**：顶层**重复标签**今天静默通过（零诊断、`EXITCODE=0`，实测），恢复顶层标签收集后将报重复标签诊断（源码判据 `Binder_Statements.vb:941-944` → `ERR_MultiplyDefined1` = BC30094）——**须补回归用例**（现 `tasks/script-top-level-crashes/test-plan.md` 的 F11 用例未覆盖） | **Fixed**（48d8edb） |
| 12 | `issue-submission-instance-constructor-crash.md` | 脚本顶层声明**实例**构造器（`Sub New()`；`Protected`/`Private`/`Public` 同判定）→ **两种症状**：构造器在首位 = 断言终止 `Debug.Assert(comparison <> 0)`（`Compilation\LexicalOrderSymbolComparer.vb:43`）`EXITCODE=2148734499`；构造器之前有任何一个成员 = `InvalidOperationException: Sequence contains more than one element`（`Symbols\NamedTypeSymbol.vb:699` 的 `InstanceConstructors.Single()`）`EXITCODE=2148734217`。根因：提交类实例分支**无条件**合成 `SynthesizedSubmissionConstructorSymbol`（`SourceMemberContainerTypeSymbol.vb:2744-2762`），而宿主独占提交实例（`GetScriptConstructor` 的 `.Single()`、`<Factory>` 体 `Dim submission As New Submission#N(submissionArray)`）⇒ 用户构造器无落点。**普通类同形状合法**（`vbc` exit 0，实测）⇒ 判「报错」，新码 `BC37342`。**漏网面**：判据原先只认 `TypeKind.Submission`，**非提交脚本类**（`DeclarationKind.Script` → `TypeKind.Class`，`IsScriptClass` 为真）声明实例构造器时同一 `InvalidCastException` 原样存在（`GetDiagnostics()` / `Emit` 均抛，已实测）；判据放宽到 `IsScriptClass`（与同文件 `:2786` 合成脚本初始化器与入口点的门一致），两类脚本类都报 | **Fixed**（已验证，commit 待作者提交后补） |
| 13 | `issue-top-level-raise-event-instance-event-crash.md` | 脚本顶层实例 `Event` 在**顶层成员**（`Sub` 或 lambda）里 `RaiseEvent` → Debug 断言终止（`EXITCODE=2148734499`）；放宽断言后落共享发射层 `CodeGen\EmitExpression.vb:206-209` 的 `UnexpectedValue`（栈顶 `PreviousSubmissionReference`）。根因：脚本类非限定成员引用一律经 `TryBindInteractiveReceiver`（`Binder_Expressions.vb:2570-2576` → `:2615-2634`）解析成 `BoundPreviousSubmissionReference`（**同一次提交内也不例外**），而 raise 的 `Else` 分支不自带降级（只在接收者为 `Nothing` 或 `Me` 时才走整棵树降级）。**普通类同形状合法且投递**（`vbc` exit 0，输出 `H`，实测）⇒ 判「修好」 | **Fixed**（已验证，commit 待作者提交后补） |
| 14 | `issue-top-level-handles-clause-crash.md` | 脚本顶层方法带 `Handles` 子句（实例与 `Shared`）→ 断言终止 `Unexpected value 'Submission'`（`Symbols\Source\SourceMemberMethodSymbol.vb:782-783`）。根因：`Select Case ContainingType.TypeKind`（`:773-784`）只列 `Class`/`Module`，`Submission` 落 `Case Else`；提交类**是**类容器（`NamedTypeSymbol.vb:691-695`），Hookup 宿主构造器（`:790-807`）对提交类同样成立。**普通类同形状合法且投递**（`vbc` exit 0，输出 `H`，实测）⇒ 判「修好」。**边界**：变量声明在**同一提交**里才受支持；来自上一提交或宿主对象的变量报 `BC37343`（`SourceMemberMethodSymbol.vb:707-711`） | **Fixed**（已验证，commit 待作者提交后补） |
| 15 | `issue-top-level-mybase-assert.md` | 脚本顶层**显式** `MyBase`（裸表达式 / 实例 `Sub` / `Shared Sub`）→ 断言终止 `Field 'type' cannot be null`（`Generated\BoundNodes.xml.Generated.vb:6016`）。根因：`BindMyBaseExpression` 的错误路径直接取 `BaseTypeNoUseSiteDiagnostics`，而提交类无基类型（`Nothing`）；同文件 `BindMeExpression`（`:2351`）/ `BindMyClassExpression`（`:2387`）用的是 `ErrorTypeSymbol.UnknownResultType` 兜底。脚本**已**报 `BC36966`，缺的只是「报完不崩」⇒ 判「修好」（保留已报诊断）。**普通 `Module` → BC32001、`Structure` → BC30044、有基类的 `Class` → 合法**（三条均实测） | **Fixed**（已验证，commit 待作者提交后补） |
| 16 | `issue-top-level-finally-branch-invalid-il.md` | 脚本顶层 `Finally` 里**任何**离开该块的分支（`GoTo` / `Return`（含带值）/ `Exit For`·`While`·`Do`·`Select` / `Continue For`；含嵌套 `Finally`、`Finally` 在 `Using`·`Catch` 内、多 `Catch`、`Await` 之后、`#Load` 进来的文件）→ 运行期 `InvalidProgramException`（`EXITCODE=2148734266`）、编译期**零诊断**。根因：顶层语句不做流分析（`Compilation\MethodCompiler.vb:625` 的 TODO），而 `ERR_BranchOutOfFinally` 的**唯一报点**在流分析 `Analysis\FlowAnalysis\ControlFlowPass.vb:151-181`（`:167` 的 `errId = …`）——grep 后重数实锤：**编译器产品源码**的 `.vb` 文件共 4 处命中（定义 `Errors.vb:166` / BuildOnly 清单 `ErrorFacts.vb:138` / 报点 `ControlFlowPass.vb:167` / 收口点的过滤读取 `Binder_Initializers.vb:292`；同目录 `VBResources.resx:492` 是文案条目，测试源码不计入）。**普通方法里同形状报 BC30101**（实测）⇒ 判「报错 `BC30101`」 | **Fixed**（已验证，commit 待作者提交后补） |
| 17 | `issue-top-level-statements-skip-flow-analysis.md` | 顶层语句（实例初始化器）**拿不到流分析警告**：顶层语句块里的局部变量未赋值即用 → 零诊断（`vbi /check`）+ 运行期 `NullReferenceException`，而同形状放进顶层 `Sub` / 嵌套类型方法 / 普通 `Module` 均报 `warning BC42104`（四路对照实测）。根因同在 `MethodCompiler.vb:625`。**剪枝**：`BC42105` **不受影响**（顶层 `Function` 与嵌套类型方法都正常报，实测）；不可达代码警告在本编译器里**没有活的报点**（`WRN_UnreachableCode` 仅 3 处命中、全在 `ControlFlowPass.vb:89` / `:100` / `:110` 且都是注释，该 id 在 `Errors.vb` 里无定义；既有两条用例断言零诊断，`BindingErrorTests.vb:21450` / `:21480`）；顶层 `Dim` 是字段，其零警告**不是**缺口 | **Open** |
| 18 | `issue-submission-shared-constructor-not-run.md` | 脚本提交类的共享构造器（`.cctor`）**不由提交构造或顶层语句触发**（只在共享字段被读 / 写时触发）：共享字段初始化器与共享 `WithEvents` 的 `Handles` 挂钩在脚本执行路径下静默不执行（exit 0、零诊断、handler 不投递；`tmp\probes\u6bc\handles-shared.vbx`）。**挂钩体本身正确**（强制类型初始化器后立刻投递，实锤）；**脚本特有**（普通同构 `tmp\probes\u10\m4-holder-go.vb` 投递 `H`，两者都 `BeforeFieldInit` + 有 `.cctor`）。根因候选：`Emit\NamedTypeSymbolAdapter.vb:482-494` 只对「`Handles` 接共享构造器」抑制 `BeforeFieldInit`，而共享 `WithEvents` 的 hookup 宿主是属性 setter（`SourceMemberMethodSymbol.vb:792-793`）。**未闭合（推测）**：脚本里方法调用不触发提交类 `.cctor` 的 CLR 层原因。副产物（实锤）：脚本里**所有**共享字段初始化器都是惰性的；既有验收探针 `shared-init-runs.vbx` 自己在顶层读了该字段，属真空控制。**未修复**（不是崩溃 / 改时机等于改语义 / 机制未闭合） | **Open** |
| 19 | `issue-cross-submission-handles-clause-crash.md` | 脚本**跨提交** `Handles` 子句（`WithEvents` 变量来自上一提交或宿主对象）→ `InvalidCastException`（`ImplicitNamedTypeSymbol` → `SourceNamedTypeSymbol`；抛点在 `SourceMemberMethodSymbol.vb` 的 `isFromBase` 分支，修复前是 `DirectCast`，现 `:707-711` 的 `TryCast` 判据），**零编译诊断**。根因：提交类的成员查找沿 `PreviousSubmission` 回溯再落到宿主对象类型（`Binder_Lookup.vb:583-584` / `:920-926`），把**上一提交**（或宿主对象）声明的 `WithEvents` 变量当继承成员返回 ⇒ `isFromBase=True`；而覆盖属性机制要求「有可覆盖的基属性 + 赋值虚派发」，提交类 `base=<nothing>`（`ImplicitNamedTypeSymbol.vb:59`）⇒ **最小回退实现实测编译通过但 handler 计数恒 `0`**。**普通 VB 里没有对应形状**（`Handles` 只能引用本类型或基类的成员；`vblang\spec\type-members.md:843` 逐字把该情形定义为编译期错误），判定原则里「同形状普通上下文合法就该修好」对它不适用 ⇒ 判「**报错**」，新码 `BC37343`；与 `meeting-with-events-in-submissions.md:120`（R5「跨提交走诊断、P-A3 覆盖属性路线否决」）与 `proposal…:439`（既有码不可复用 ⇒ 必须新增码）一致 | **Fixed**（已验证，commit 待作者提交后补） |
| 20 | `issue-submission-custom-event-raise-assert.md` | 脚本提交类的**自定义** `Custom Event` 在成员体内 `RaiseEvent` → Debug 断言终止（`EXITCODE=2148734499`，栈顶 `Lowering\LocalRewriter\LocalRewriter_RaiseEvent.vb:33` 的 `Debug.Assert(receiver.Kind = BoundKind.FieldAccess)`；宿主侧同形状抛 `TraceAssertException`）。根因：**自定义事件没有后备字段**（raise 调的是事件自己的 `RaiseEvent` 访问器 ⇒ 接收者是实例引用），而 `Else` 分支假定接收者是事件字段访问；提交类的非限定成员引用又解析成 `BoundPreviousSubmissionReference`（`Binder_Expressions.vb:2615-2618`）⇒ 前提双重不成立（与 issue 13 的断言**不是同一条**）。**普通类同形状合法且投递**（`vbc` exit 0、`COUNT=2`，实测）⇒ 判「修好」：判据扩宽到 `PreviousSubmissionReference`，让自定义事件走 `If` 分支（`LocalRewriter_RaiseEvent.vb:27`） | **Fixed**（已验证，commit 待作者提交后补） |

> 状态约定：**Open**（待修复）/ **In Progress**（已认领）/ **Fixed**（已验证修复，注明 commit）。
> 修复已通过验证但**尚未提交**时，记 `**Fixed**（已验证，commit 待作者提交后补）`——**不得**预填/推测 commit 号；提交后补齐。
> 本清单的 issue 只记**触发面 + 根因 + 预期**；修复后的**新增行为变化**（今天静默、修复后会报诊断/改变产物的形状）必须写进对应 issue 正文的小节，并注明须补的回归用例。
