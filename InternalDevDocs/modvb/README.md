`ModVB` is a product of Anthony D.Green. See `upstream.url` and `AnthonyDesign_wordpress.txt` (the offline cache) for more information.

LDM meetings are generated by AI (e.g. DeepSeek). They can only be used as AI input materials of `VBScript.NET`. Other usages are unsupported.
