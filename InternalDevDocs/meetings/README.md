# VBScript.NET 产品会议索引

本文档登记 **VBScript.NET 产品自身**的设计会议（meetings 层）。会议纪要评估对应的产品提案（`../proposals/`），产出「**LDM 状态** + **三态判定**」，是提案状态分类（active/inactive/rejected/done）的判定依据。

**与 modvb 的关系**：`../modvb/meetings/` 是 Anthony 提案库的 LDM 会议纪要（AI 生成的参考材料）；本目录是 VBScript.NET 产品自身的设计会议，与产品提案 1:1 同步组织。

## 目录组织

- `meetings/` ↔ `proposals/`
- `meetings/inactive/` ↔ `proposals/inactive/`
- `meetings/rejected/` ↔ `proposals/rejected/`
- `meetings/vbscript-<版本>/` ↔ `proposals/vbscript-<版本>/`（done 归档，当前无成员）
- 会议做三态判定/优先级时，可对照 `../dream-of-vbdev.md`（§2 主线、§5 六元素、§7 三方张力）作动机/定位上下文（非权威输入）。

## 会议纪要

| 文件名 | 对应提案 | 主题 | 三态判定 |
|--------|---------|------|---------|
| `meeting-vb-repl-parity-with-csharp-repl.md` | 现状盘点 + 评估 `proposal-optional-question-prefix.md` 与 `proposal-avalonia-ise-repl-ui.md` | 让 VB REPL 的功能追上 C# REPL 的讨论（2.0 beta 现状盘点、借鉴 csharplang、评估两个新提案） | optional-question-prefix = **Active**；avalonia-ise-repl-ui = **Consider** |
| `meeting-optional-question-prefix.md` | 评估 `proposal-optional-question-prefix.md`（REPL `?` 可选） | 深度评审：REPL 表达式开头问号可选——源码机制核实（PrintStatement / BC30545 / HasSubmissionResult）、候选方案（A/B/C/D）、实现落点（编译器层，跟随 C# REPL 设定）、边界与诊断族枚举 | optional-question-prefix = **Active** |
| `meeting-vscode-extension-ise-repl-ui.md` | 评估 `proposal-vscode-extension-ise-repl-ui.md`（VS Code 扩展 REPL/脚本编辑器） | 深度评审：VS Code 扩展运行方式全考虑（A 一进程 REPL+LSP+DAP / B 独立 stdio LSP / C Zed 扩展 / D 仅编辑）、Zed 竞品分析（本体内存低、stdio LSP、tree-sitter）、LSP 宿主作为跨客户端资产、与 Avalonia 双路线互补 | vscode-extension-ise-repl-ui = **Consider** |
| `meeting-byref-like-repl-safety.md` | 前置议题：为 `../proposals/proposal-byref-like-safety.md`（已建，byref-like 安全）与 `../tasks/p1-immediate.md` 前置-1（D1 RefStructHelper 移植 + suppress obsolete error）铺路 | 现状盘点（REPL submission 机制源码核实：顶层 `Dim`=脚本类字段、末尾表达式装箱到 Object、`<Initialize>` 恒 async）+ 三个碰撞点（字段持久化/结果打印/跨 Await）+ C# 借鉴（span-safety、CSX 持久化冲突）+ 定案：byref-like 提交内可用、不跨提交持久化，顶层变量/结果/跨 Await 编译错误 | 方向定案（D1 REPL 侧语义契约）；提案 `proposal-byref-like-safety.md` = **Active（Proposed）** |
| `meeting-vbscript-lsp.md` | 评估 `../proposals/proposal-vbscript-lsp.md`（vbscript.net LSP：普通 VB 项目 + VBX 脚本） | 深度评审：vb-ls 成本基线（2 patch + launcher）、本地基线补层机制四证据（public API 一致 / IVT 不授 IDE 栈 / 程序集身份一致 / Arcade 依赖使拷贝直引不可行）、两模式唯一区别是 script mode、语言服务器独立进程 + dotnet tool 分发（LDM 拍板）、里程碑 M0–M4 | vbscript-lsp = **Active（Proposed）** |
| `meeting-shebang-directive.md` | 评估 `../proposals/proposal-shebang-directive.md`（`.vbx` 首行 `#!` shebang） | 深度评审：C# 对等语法 LDM 决策链（issue #3507 → LDM-2020-07-20/09-28 → C# 14 ignored-directives #8617，`#!`/`#:` 为 ignored 指令）、VB 落地表面（上游 `#R` 管道为模板、词法零改动、整行消费是唯一新增机械件）、模式门控（仅 script）、位置规则（首字符 + BOM 不能在前）、severity（spec warning vs 实现 error） | shebang-directive = **Active（Proposed）** |
| `meeting-consume-csharp-extension-and-interface-shared.md` | 评估 `../proposals/proposal-consume-csharp-extension-and-interface-shared.md`（消费 C# 扩展成员与接口共享成员） | 深度评审（vblang 官方叙事结构）：四层延申消费扩展成员 + SAIM 约束接口绑定；**源码锚点逐一复核**并发现运行时钩子已在位（`RuntimeCapability.VirtualStaticsInInterfaces`）与 `ExtensionMarkerNameAttribute` 术语对齐；范围定案消费/声明分离；独立五维评审为 git-ignored 工作材料、不入库 | consume-csharp-extension-and-interface-shared = **Active** |
| `meeting-distribute-compiler-nuget-package-and-dotnet-tool.md` | 评估 `../proposals/proposal-distribute-compiler-nuget-package-and-dotnet-tool.md`（分发 fork 编译器：Toolset NuGet 包 + .net tool） | 深度评审（vblang 官方叙事结构）：分发现状源码核实（三库 IsPackable 无产物、MSBuildTask/VBCSCompiler 被裁剪、进程内编译回退在 `BuildClient.cs:148-165`）；四条 Unresolved 备选列全调查后闭合（U1 隐式分派 / U2 不补 VBCSCompiler / U3 v1 仅 .NET SDK / U4 双行版本）；发现 `vbi --version` 链路已就位（`Vbi.vb:31-50`）且版权文案已「不提及 Microsoft」；独立五维评审为 git-ignored 工作材料、不入库 | distribute-compiler-nuget-package-and-dotnet-tool = **Active** |
| `meeting-vbi-script-diag-mode.md` | 评估 `../proposals/proposal-vbi-script-diag-mode.md`（vbi 脚本模式诊断检查 `/check`） | 深度评审（vblang 官方叙事结构，多参会者流程）：现状缺口实证（成功路径警告被吞 / 错误中止 / vbc 专属开关 BC2007）；发现 `Script.Compile()` 现成 API + 交互模式同款编译前置已在使用；候选方案 A–E（`/check` 采用 vs `/diag` `/norun` `/analysis` 维持现状）；**命名裁决 `/diag`→`/check`**（AI 先验标准：`/diag` 撞 msbuild 详细日志、方向相反）；四 Unresolved 备选列全调查后闭合（U1 严重度配置=v1 不带 / U2 互斥=/check 优先 / U3 MSYS=排除转换 / U4 内存 emit=接受）；独立五维评审报告为 git-ignored 工作材料、不入库 | vbi-script-diag-mode = **Active** |
| `meeting-consume-ref-readonly.md` | 评估 `../proposals/proposal-consume-ref-readonly.md`（消费 C# `ref readonly` 返回） | 深度评审（vblang 官方叙事结构，多参会者流程）：根因收敛到一个 modreq（`CMOD_REQD(In) BYREF T`）；发现 C# 既有结构 `PEPropertySymbol.cs:363-367` 已白名单 InAttribute；候选方案 A–E；裁决：**统一豁免返回+参数双路径 In modreq**（csc 发射实证虚方法/委托 `in` 参数带 modreq）、直接赋值**复用 `ERR_LValueRequired`（30068）零新码**（显示退化自洽 + 30098→30068 既有迁移；拒绝面含复合赋值/Mid，VB 老登新增发现）、tooltip 显示退化 + `ByRef ReadOnly` 仅 debug 格式、丢弃写回接受（与字面量传 ByRef 一致）、`ReturnsByRefReadonly` 填既有占位、For Each 解锁、§4a v1 不做；C# 7.2 归属修正；独立五维评审为 git-ignored 工作材料、不入库 | consume-ref-readonly = **Active** |
| `meeting-vbi-nuget-reference.md` | 评估 `../proposals/proposal-vbi-nuget-reference.md`（.vbx/REPL `#R "nuget:"` NuGet 包引用） | 深度评审（vblang 官方叙事结构，双老登流程 + 复会 1/2）：**定性**=宿主/工具链契约而非 VB 语言特性（归 M5、进产品 spec 不进 vblang spec）；引擎 **C1 采用**（dotnet CLI 内容寻址临时工程 restore）、C2 与增量自解析否决；语法**逗号 + 大小写不敏感前缀**（附两义务：近失配显式诊断、共享层文档标注防 merge 误回滚）；**接线 R4 经复会 1 改判**——共享 Core 单 `#R` 只收 1 解析结果（`CommonReferenceManager.Resolution.cs:883-887` 上游 `// TODO: implement`），改为**扩展共享 Core 支持单 `#R`→N 引用**为 U9 spike 目标架构（受 spike 硬门控；不 alter 旧 `CSharpCompilation.GetDirectiveReference`、N 语义走新增 API/路径，产品 VB-only 故 C# 零 churn；spike 不过回退宿主拦截=干净剥行过渡）；宿主驱动环落点订正在共享 `CommandLineRunner.RunInteractiveLoopAsync`（`CommandLineRunner.cs:240-310`）；U1-B（锚点订正 `Scripting.Core.csproj:54-58`）/U2-A/U3-B/U4-A/U5-A/U6-A/U7-A/U8-A 采纳；**TFM 镜像经复会 2 收窄为只镜像运行宿主 TFM**（`' Attribute TargetFramework` 由 vbichooser 派发、vbi 引擎不读，TFM 镜像 spike 删除）；**U5 native 经复会 3**——net10 native 支持（sqlite 类先行 + Scripting loader seam `ResolvingUnmanagedDll` spike 验收；宿主供策略目录、机制落 loader seam），WindowsAppSDK 类 net10 后置，net48 范围外（R6 锚 `#R` 宿主能力诊断）；测试策略为新增非改断言；独立五维评审为 git-ignored 工作材料、不入库 | vbi-nuget-reference = **Active** |
| `meeting-vbi-project-reference.md` | 评估 `../proposals/proposal-vbi-project-reference.md`（.vbx/REPL `#R "project:"` 本地工程引用） | 深度评审（vblang 官方叙事结构，双老登流程）：**定性**继承近邻 R1（宿主/工具链契约、第三种宿主解释前缀、归 M5、进产品 spec）；引擎 **P1 采用**（spawn 真实 `dotnet build` 用户工程、SDK 增量 up-to-date；P2 进程内 MSBuild / P3 约定猜路径否决）；接线 **A1+A2a 采用**（宿主组合协调器共享 seam 形状不变 + 共享 resolver 并行 `ProjectResolver` 抽象缝、宿主只读会话），**共享编译器 Core 零改动**（继承已落地 N>1，`CommonReferenceManager.Resolution.cs:833-848`）；闭包 **范围乙**（主产物 + ProjectReference 输出 + 工程自身 NuGet 依赖）为 v1 目标、两 spike 前置为硬条件；**U1 收口 always-spawn**（删会话内 skip；REPL 内改工程需重启会话 = CLR 同 identity 不可替换新 Drawback）；U8 弃 stdout `->` 解析（MSBuild 本地化）、主路线 `-getProperty:TargetPath` 回退 `FileListAbsolute.txt`；U4 v1 收窄为宿主短 TFM 精确在列；E10 证据订正（`u9-fixtures` 当前非 ProjectReference 链）；`RuntimeMetadataReferenceResolver.Equals/GetHashCode` 纳入新 `ProjectResolver` 字段；独立五维评审为 git-ignored 工作材料、不入库 | vbi-project-reference = **Active** |
| `meeting-scripting-dialect.md` | 评估 `../proposals/proposal-scripting-dialect.md`（脚本方言的声明与提交模型） | 深度评审（vblang 官方叙事结构，双老登流程）：`<Factory>` 签名订正（`As T`→`Task(Of T)`，上游源码注释 `:336` 被当签名抄录）；显式 `Me`/`MyBase`/`MyClass` 禁令范围裁决——判据收窄到脚本初始化器/构造函数（顶层成员体回归普通实例方法语义，顶层语句仍禁显式 `Me`）；`Imports` 跨提交累积写成对 `source-files-and-namespaces.md:253` 的显式方言偏离（补同别名不同目标/XML 前缀重定义/`Global` 前缀三类规范化 + 累积失败降级为诊断）；补「脚本编译选项默认」一节（`Option Strict Off`/`Option Infer On`/`Option Explicit On` = 晚绑定基因载体）；诊断族补 BC42367 与顶层标签/`GoTo` 并逐条标注 C# 对应物；§8 末尾表达式收窄基准改写为「与 C# 顶层语句同向、与共享 `Script<T>` 有意分叉」；独立五维评审为 git-ignored 工作材料、不入库 | scripting-dialect = **Active** |
| `meeting-script-optimization-level.md` | 评估 `../proposals/proposal-script-optimization-level.md`（脚本编译优化级别：`/optimize` 对脚本生效） | 深度评审（vblang 官方叙事结构）：现状断点源码核实（`/optimize` 解析位于 `VisualBasicCommandLineParser.vb` 非脚本分支、脚本模式原落 `WRN_BadSwitch` BC2007；`CommandLineRunner.cs:177` 硬编码 Debug）；候选方案 A–D；定案 A 两处改动（C1 宿主透传 `arguments.CompilationOptions.OptimizationLevel` + C1b 脚本专属分支新增 `/optimize`、`/optimize+`、`/optimize-`）；传递途径定案命令行参数 + rsp（环境变量 / 脚本头指令不采用，用户定案 2026-08-22）；`/debug` 不透传；`#If DEBUG` 语义定案（`/optimize+` 即 Release 语义）；姊妹提案 `proposal-vbi-script-diag-mode.md`（同为脚本模式加开关）；实现 A1/A2 + H1-H9 共 11 用例全绿、`Scripting\VisualBasicTest` 194 全量 0 失败 | script-optimization-level = **Active** |
| `meeting-reference-directive.md` | 评估 `../proposals/proposal-reference-directive.md`（`#R` 引用指令本体） | 深度评审（vblang 官方叙事结构，双老登流程）：**定性**=「编译器语法 + 宿主/工具链契约」而非 VB 语言特性（记录型提案，机制随 1.2 发布、单 `#R`→N 已登账）；别名三事实（VB 无 `extern alias` 逃生口 / 被别名引用在源码中不可达 / `/nostdlib` 常规编译同属功能语义）；公共观察点订正（N 完整集可从公共 `Compilation.DirectiveReferences` / `Compilation.References` 观察，非仅 `ExplicitReferences`）；解析顺序钉死（`IsFilePath` 精确判据 + `ResolveReference`/`ResolveMissingAssembly` 两条契约不统一）；单 `#R`→N 的去重交互（闭包是否进公共面取决于是否已被别处引用过）；C# `#r` 对照改引（位置约束改引 `DirectiveParser.cs`、补 `#:` 与 `FileBasedProgram` 门控）；15 条 RESOLUTION；独立五维评审为 git-ignored 工作材料、不入库 | reference-directive = **Active** |
| `meeting-load-directive.md` | 评估 `../proposals/proposal-load-directive.md`（`#Load` 源文件加载指令） | 深度评审（vblang 官方叙事结构，双老登流程）：**定性**=「编译器语法 + 宿主展开契约」的组合、非 VB 语言特性（记录型提案，机制随 2.0 beta 落地、2.16 与 NuGet 预扫描共用展开点）；`#Load` 不进声明表（对照 C# `HasReferenceOrLoadDirectives` 把 `#load` 当引用集合信号）；失败通道订正（提案两条 Drawback 不成立——`Script.Compile()` 把 `CompilationErrorException` 转诊断、`/check` 契约成立、REPL 丢本次提交不崩会话）；环/缺失分码与菱形去重列为实施项（提案「分码需要额外状态」「去重会歧义」两条理由被源码推翻）；公开 API 台账缺项从 `Suspect` 升为实锤（`LoadDirectiveTriviaSyntax` / `LoadKeyword` / `GetLoadDirectives` 零命中）；C# 对照补六行（引用管理器复用 / 空操作数 / 读取失败 / resolver 为空 / 树序 / 大小写与 `IsActive`）；独立五维评审为 git-ignored 工作材料、不入库 | load-directive = **Active** |
| `meeting-script-extension-methods.md` | 评估 `../proposals/proposal-script-extension-methods.md`（脚本类中扩展方法的合法形式与缺失诊断） | 深度评审（vblang 官方叙事结构，双老登流程）：**裁决 B + F + H 采纳、A/C/D/窄 D/E-a/E-b/E-c 否决、G 表为可选**；**发现缺口②的根因是 VB 内部两个谓词分裂**（诊断侧 `AllowsExtensionMethods` 不说嵌套 vs 收集侧 `MightContainExtensionMethods` 要求容器在命名空间层，`SourceMemberContainerTypeSymbol.vb:3341`），据此 F 不是「新诊断」而是**把 C# 的嵌套检查（CS1109，`SourceOrdinaryMethodSymbol.cs:230-232`，不豁免脚本类）补回来**（§7「嵌套类型」行映射错误被两条路径独立命中；VB 消费侧 `PENamedTypeSymbol.vb:921-936` 写着「only top level」为旁证）；**C 与窄 D 的代价被提案低估**——`.vbx` 单提交是主场景（C 拿走全部、脚本无 `Namespace` 可改道），窄 D 破坏 `spec-scripting-dialect.md:125` 明文承诺且**顶层 `Module` 在 REPL 跨提交可见已实测**（`? M.F()` → 7）；H 取形态 (a)（对齐 `InSubmissionClassBinder`、装入条件 `IsSubmissionClass`）并收窄到经典扩展方法一路（扩展成员一路属 VB 独有，C# 脚本不可声明 → CS9283）、**前置须先补登 `upstream-merge.md` 的 e307d0f 既有分歧**；跨提交 proximity 问法更正（C# 零命中，须定义在 VB 自身语义内）；**新增机制发现**：`ShouldLookupExtensionMethods` 实为三条子句（`Not container.IsShared` 因 `TypeSymbol.IsShared` 恒 `False` 而空转、`Not container.IsModuleType()` 额外排除模块接收者）、宿主在 `VisualBasicScriptCompiler.vb:218` 写死 `Option Strict Off`、`DeclarationTreeBuilder.vb:192` 与 C# `:292` 注释逐字相同而语义相反（陈旧补丁迹象，`Suspect`）、既有常规 VB 断言 `ExtensionMethodTests.vb:1061` 钉住嵌套模块；诊断一律落声明点（四种按接收者的失败形态）；测试须避开 `Object` 接收者盲区（非 `Object` + 断返回值 + 负例）；**2026-09-12 聚焦复会**（delta＝「容器机制上是 class／心智是 module」的错配要不要换容器种类）——两路复会意见在四形态上一致：**①（＝G）维持 Table**（对「用它消解容器错配」再加一道否决口径；强调级别保留／附带条件支持，置信度高／中）、**①b 独立定名并否决**、**② 状态静态化否决**（反对，置信度高／中）、**③ 容器改 Module 否决**（反对，两路同给高：③-甲 源码级不可行、③-乙 是「第三种种容器」的语义换血、跨提交查找入口按 `TypeKind` 分派故比已否的窄 D 更重、D5 容器种类维反向）、**④ 采纳并升为叙事前置**（两路同给强推／高；新增 ④-iv 诊断措辞承载叙事）；容器**保持 `TypeKind.Submission`**；新增 R21（delta 裁决）/R22（①b 独立否决）；复会出两条**实测**（顶层 `Shared` 状态跨提交读写均可用；p10 `TypeLoadException` 栈帧逐字到 `ScriptBuilder.cs:194`）与两条**材料勘误**（③-甲「构造函数断言失败」与「两谓词矛盾」两条崩点理由**作废**——断言不可达、两谓词一起变假，真正的致命点是 `IsScriptClass` 变假 ⇒ `MethodCompiler.vb:564` 整块不执行；`Binder_Expressions.vb:2612` 的 `currentMember` 是**引用点所在成员**、被材料读反）；另钉两处口径（`CodeGen`/`Lowering` 的 Module 类判据 0/0、Submission 类判据 1/0；会话隔离句应为 `spec:361` 而非 `:354`）；独立五维评审为 git-ignored 工作材料、不入库 | script-extension-methods = **Active（Proposed）**（2026-09-12 聚焦复会：容器种类裁定「保持 `TypeKind.Submission`」，④ 升为叙事前置） |
| `meeting-with-events-in-submissions.md` | 评估 `../proposals/proposal-with-events-in-submissions.md`（提交里的 `WithEvents` / `Handles`） | 深度评审（vblang 官方叙事结构，双老登流程）：四条输入路径复核，其中 (a)(c)(d) 在 Debug `5816a5c` / Release `e307d0f` 两个二进制上重跑复现（Debug 断言退出码 35 / Release 9 与 1）；`:70` 逸出栈经 Release `e307d0f` 二进制实测复现判定归属，并拆成「文件脚本栈」与「REPL 栈」两条——两个被撤的自补帧从「无依据」改判为**另一条栈的真帧**；「未处理异常」订正为被 runner 兜底 `catch` 接住（退出码 9 = `HResult` 低字节）；补跨提交 `Handles` 因果链两锚点（绑定层实际违反 `type-members.md:843`）；裁决 **P-A1 采纳 +「同一次提交」升格为规范边界 + 候选 C 与 P-A1 是补集（捆绑）+「同名替换/自动摘钩」否决 + P-A3 否决（规范与元数据两侧证不可表达）+(iii) 出诊断 +(vi) 并入同一条门**；回归网 `CodeGenWithEvents.vb` 确认在树内；独立五维评审为 git-ignored 工作材料、不入库 | with-events-in-submissions = **Active（Proposed）** |
| `meeting-submission-shared-members.md` | 评估 `../proposals/proposal-submission-shared-members.md`（提交/脚本类里的 `Shared` 成员） | 深度评审（vblang 官方叙事结构，双老登流程）：承重锚点逐一复核全部命中（钉清 `SynthesizedConstructorBase.vb` 的 `MethodKind` 承重行为 `:192`、非 `:193`；纠两处行号偏移与「复验日志与冻结提案不同步」四例）；判别性实测把「静态桶有无条目」锁死 issue 05（无初始化器 → exit 0 / 仅数组上界 → exit 34）；**发现提案未列的机制缺口**——`EmitAddress.vb:261-284` 的 `HasHome`（共享只读字段只在 `MethodKind.SharedConstructor` 里有家）+ `FieldSymbolAdapter.vb:160-166` 与 `MetadataWriter.cs:761-764` 的 `initonly` 发射 ⇒ **「丙 同时消掉 issue 05」只在非只读这一半成立**（两路老登独立撞上、探针方向不同结论相同）；**发现绑定上下文与执行上下文已存在的缝**（`DeclarationInitializerBinder.vb:54-58` vs 实例方法 `<Initialize>`，Unresolved 3 的 `BC30469` 是其症状）；**发现作用域未钉**（丙 的分桶点不含容器种类门，会连带改掉今天健康的非提交脚本类形状，`vbc` script 模式可达）；补 Release 观测（较旧 `e307d0f` 参照级：`Await` 形状 exit 9 抛 `InvalidOperationException` 非静默错产物；顶层 `Event`/`WithEvents` **exit 0 正常**⇒ 支持 issue 07 的放宽断言方向）；**裁决推翻提案主候选**——**甲 + 乙 捆绑采纳**（甲＝D5 已证实例 1 逐点复制、两个符号而非形参分叉，落点 `:2726-2737` 按 `isShared` 分叉到 `EnsureCtor`；乙＝issue 06 目标定为「从崩改成报错」，三条件：新脚本专属码 / 覆盖共享属性 / 改调用结构，硬事实「`GetAwaitInNonAsyncError` 加分支不可达」经复核成立）、**丙 否决**（非兼容性理由：`Shared` 概念改义（`type-members.md:1269`/`:2021` 为类型级先于首次引用，且同一 `Script` 重跑使非 `Await` 形状由一次性变每次运行）/ 核心收益不完整 / 收益面窄（`Binder_Expressions.vb:2612` 仍挡住宿主与前序提交）/ 与 D5 同形性反向；附复活条件）、丙-2 否决（排序语义必破 + 新增 C# 侧不存在的入口点成员）；issue 07/08/09 登记接受、独立立项；R17 措辞订正指向 issue 05 候选 B；独立五维评审为 git-ignored 工作材料、不入库 | submission-shared-members = **Active**（甲 + 乙 采纳；丙 / 丙-2 否决） |

## Inactive（`meetings/inactive/`）

| 文件名 | 对应提案 | 主题 | 三态判定 |
|--------|---------|------|---------|
| `inactive/meeting-vbi-console-completion.md` | 评估 `../proposals/inactive/proposal-vbi-console-completion.md`（vbi 控制台 REPL 行内补全） | 深度评审（vblang 官方叙事结构，双老登流程）：源码锚点整条复核命中；纠偏三处表述（Vbi.vb:86 输出侧≠输入侧谓词 / 多行态是真多行编辑器 / Windows-Unix 平台 ROI 不对称）+ dotnet/interactive ≈310 行 glue 改读为"工程级补全 = Features 层"反证；**裁决 Table**（此刻捆绑投入 L0+L1 不成立——补全能力将由已 Active 的 LSP 补层 / Consider 的 Avalonia/一进程编辑面承接）；L0/L1 拆分、剥离保留三件低成本资产、复活闸门 G1（现成读行库采纳）/G2（LSP 补层 M0/M1 落地接 Features top-1）/G3（浅档探针命中率/延迟基线）；修正点 a–f 记纪要不写回提案；独立五维评审为 git-ignored 工作材料、不入库 | vbi-console-completion = **Table（归档待触发）** |

## 会议纪要格式

- 标题：`# Visual Basic Language Design Meeting` + 日期行。
- 结构：开场白 → `## Agenda` → 各 Proposal 讨论（场景与缺口 / 候选方案 / 权衡 Q&A / RESOLUTION / 状态）→ 附录。
- 状态：每个讨论项给 **LDM 状态** + **三态判定**（仿 modvb meeting 写法）。
